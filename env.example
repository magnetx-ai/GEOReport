# Optional. The CLI runs without any of these (site GEO hygiene only).
# Copy to .env locally if you want; the CLI also reads process environment.

# OpenAI-compatible Chat Completions (raw answers only; no analysis)
MAGUP_LLM_API_KEY=
# OPENAI_API_KEY=          # also accepted
MAGUP_LLM_BASE_URL=https://api.openai.com/v1
MAGUP_LLM_MODEL=gpt-4o-mini

# DataForSEO (raw JSON dump only; not MagUp Search Profile)
DATAFORSEO_LOGIN=
DATAFORSEO_PASSWORD=
